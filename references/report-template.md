# 报告模板

```markdown
# {项目名称} 安全审计报告

## 审计概要

| 项目 | 内容 |
|------|------|
| 审计日期 | YYYY-MM-DD |
| 审计范围 | {全量/指定模块} |
| 项目规模 | {小型/中型/大型}（EALOC = {数值}） |
| 语言/框架 | {语言} / {框架} |
| 审计工具 | vibe-csa v1.0 |
| 审计方法 | 白盒代码审计（Semgrep 静态分析 + 双轨数据流追踪 + CoT 逻辑推理） |

## 审计进度表

| Tier | 描述 | 文件数 | 已审查 | 覆盖率 | 阈值 | 状态 |
|------|------|--------|--------|--------|------|------|
| T1 | 入口点 | {n} | {n} | {x}% | 100% | ✅/❌ |
| T2 | 业务逻辑 | {n} | {n} | {x}% | {x}% | ✅/❌ |
| T3 | 数据结构 | {n} | {n} | {x}% | {x}% | ✅/❌ |
| 总体 | — | {n} | {n} | {x}% | {x}% | ✅/❌ |

## 漏洞统计

| 严重程度 | CONFIRMED | HYPOTHESIS | 合计 |
|----------|-----------|------------|------|
| Critical | {n} | {n} | {n} |
| High | {n} | {n} | {n} |
| Medium | {n} | {n} | {n} |
| Low | {n} | {n} | {n} |
| 合计 | {n} | {n} | {n} |

## 漏洞列表

| 编号 | 标题 | 严重程度 | DKTSS | 状态 |
|------|------|----------|-------|------|
| {C/H/M/L}-{TYPE}-{SEQ} | {简要标题} | {Critical/High/Medium/Low} | {分数} | CONFIRMED/HYPOTHESIS |

---

## {C-001}: {漏洞标题}

### 描述

{约 100 字概述：什么类型的漏洞、在哪个文件/方法、什么影响。}

### 详细分析

**调用链**：

1. `{ClassName.method()} at {path}:{line}` — {说明}
2. `{ClassName.method()} at {path}:{line}` — {说明}
3. `{Sink 调用} at {path}:{line}` — {说明}

**数据流**：

- Source: `{参数名}` 来自 `{请求参数/Body/Header}`
- Transform: `{变量名}` 在 `{file}:{line}` 被 `{操作}`
- Sink: `{危险函数}({参数})` 在 `{file}:{line}`

**缺失的安全控制**：

| 应有控制 | 实际状态 | 影响 |
|----------|----------|------|
| {如：参数化查询} | {如：使用字符串拼接} | {如：SQL 注入} |

**攻击路径**：

1. 发送 {HTTP Method} 请求到 `{URL}` with `{payload}`
2. 参数 `{name}` 未经 {过滤/编码} 直接传入 `{函数}`
3. 执行 `{危险操作}`，导致 `{影响}`

**对比分析**：

{项目内其他位置相同操作采用了安全措施（举例），与此处形成对比。}

**漏洞分类**：CWE-{编号} — {CWE 名称}

**评分**：

| 指标 | 值 |
|------|-----|
| DKTSS 优先级 | {分数} ({Critical/High/Medium/Low}) |
| 严重程度 | {Critical/High/Medium/Low} (CVSS {分数}) |
| Reachability (R) | {0-3} — {说明} |
| Impact (I) | {0-3} — {说明} |
| Complexity (C) | {0-3} — {说明} |

**证据引用**：

- `EVID_{TYPE}_EXEC_POINT`: `{file}:{line}`
- `EVID_{TYPE}_PARAM_SOURCE`: `{file}:{line}`
- `EVID_{TYPE}_NO_SANITIZE`: `{file}:{line}`
- ...

**修复建议**：

{具体修复方案，包含代码示例。}

**代码位置**：

`{完整绝对路径}:{行号}`

---

## {H-001}: {漏洞标题}（HYPOTHESIS）

### 描述

{约 100 字概述}

### 分析

{简要分析，说明可疑之处}

### 不确定因素

- {因素 1}
- {因素 2}

### 人工验证建议

{具体验证步骤}

---

## 附录

### A. 审计覆盖文件清单

#### T1 入口点（{n} 个文件）

- `{path/to/Controller1.java}`（{n} LOC）
- ...

#### T2 业务逻辑（{n} 个文件）

- ...

#### T3 数据结构（{n} 个文件）

- ...

### B. 第三方组件安全状态

| 组件 | 版本 | 已知漏洞 | 影响 |
|------|------|----------|------|
| {组件名} | {版本} | {CVE 编号} | {描述} |

### C. 未覆盖文件清单（如有）

#### T2（{n} 个文件）

- ...

### D. 审计工具配置

- Semgrep 规则数：{n}
- 危险模式匹配数：{n}
- 审计时间：{n} 小时

```
